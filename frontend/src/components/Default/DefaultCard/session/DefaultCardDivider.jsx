import React, { memo } from "react";
import { Divider } from "primereact/divider";

const DefaultCardDivider = () => {
  return <Divider className="mt-0" />;
};

export default memo(DefaultCardDivider);
